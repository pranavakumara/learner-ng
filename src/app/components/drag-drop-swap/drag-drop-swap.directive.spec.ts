import { DragDropSwapDirective } from './drag-drop-swap.directive';

describe('DragDropSwapDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropSwapDirective();
    expect(directive).toBeTruthy();
  });
});
